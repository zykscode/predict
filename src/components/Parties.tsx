import React from 'react';

import Candidate from './Candidate';

const Parties = () => {
  return (
    <section>
      <Candidate name={''} party={''} position={''} constituency={''} />
    </section>
  );
};

export default Parties;
